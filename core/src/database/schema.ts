/**
 * Drizzle schema — the source of truth for the database structure and the input
 * to migration generation (`npm run db:generate`).
 *
 * Intentionally empty in Phase 0: the foundation wires the migration pipeline
 * and connection, and the first tables (orgs, users, projects, queues, retry
 * policies) land in Phase 1. Export table definitions from here as they are added.
 */
export {};
