# Web dashboard environment.
#
# During frontend-first development the app talks to a relative "/api/v1" path
# served by the MSW mock backend, so no variables are required to run `npm run dev`.
#
# When the real core API lands, point the client at it here (and disable MSW):
# VITE_API_URL=http://localhost:3000/api/v1
